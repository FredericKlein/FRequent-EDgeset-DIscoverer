#ifndef FOUND_H
#define FOUND_H
#include<Graph.h>

class Found
{
    public:
        std::vector<Graph> edge_sets;
        Found();
        virtual ~Found();

    protected:

    private:
};

#endif // FOUND_H
